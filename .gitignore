# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.eggs/
build/
dist/

# Env / secrets
.env
.env.local
.env.*.local

# Handoff: standalone-скрипты с зашитыми токенами для команд —
# не пушим в публичный репо. Файлы передаются коллегам напрямую.
handoff/

# Test / cache
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Model caches (BGE-M3 / ONNX скачиваются в рантайме)
.cache/
models/

# Docker volumes (на случай локального bind-mount)
qdrant_storage/
pgdata/

# Локальный режим без Docker: embedded-Qdrant и SQLite-файлы
qdrant_*/
*.db
*.sqlite
*.sqlite3
# Папка локального smoke-прогона (SQLite + embedded Qdrant + временные скрипты)
.local_run/

# Data files from group 1 (KB)
*.jsonl
*.png
chunk_*.png
data/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Сгенерированный gRPC-код держим в репо? — нет, генерируем (см. scripts/gen_proto)
src/elion_dal/grpc_gen/*_pb2.py
src/elion_dal/grpc_gen/*_pb2.pyi
src/elion_dal/grpc_gen/*_pb2_grpc.py